apiVersion: apps/v1
kind: Deployment
metadata:
  name: register-pg
spec:
#  replicas: 1
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: register-pg
  template:
    metadata:
      labels:
        app: register-pg
    spec:
      containers:
      - name: register-pg
        image: harbor.k-tech.cloud/msa/register_pg:3
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: docker-config-secret
---
apiVersion: v1
kind: Service
metadata:
  name: register-pg-service
spec:
  selector:
    app: register-pg
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP